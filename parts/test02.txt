rem; color: var(--muted); line-height: 1.4; }
  
  .count-row { display: flex; gap: 12px; margin-bottom: 24px; flex-wrap: wrap; justify-content: center; }
  .count-badge {
    background: var(--card); border: 1px solid var(--border);
    border-radius: 8px; padding: 8px 16px; cursor: pointer;
    font-size: 0.85rem; transition: all 0.2s;
  }
  .count-badge:hover { border-color: var(--accent); }
  .count-badge.selected { border-color: var(--accent); background: var(--card2); color: var(--accent); font-weight: 700; }
  
  .btn {
    display: inline-flex; align-items: center; gap: 8px;
    background: linear-gradient(135deg, var(--accent), #8b5cf6);
    color: white; border: none; border-radius: 10px;
    padding: 14px 32px; font-size: 1rem; font-weight: 700;
    cursor: pointer; transition: all 0.2s; text-decoration: none;
  }
  .btn:hover { transform: translateY(-1px); box-shadow: 0 4px 20px rgba(108,99,255,0.4); }
  .btn:active { transform: translateY(0); }
  .btn-outline {
    background: transparent; border: 2px solid var(--border);
    color: var(--text); padding: 10px 20px; font-size: 0.9rem; font-weight: 600;
  }
  .btn-outline:hover { border-color: var(--accent); color: var(--accent); box-shadow: none; }
  .btn-sm { padding: 8px 18px; font-size: 0.85rem; }

  /* Quiz */
  .quiz-wrap { max-width: 720px; margin: 0 auto; padding: 32px 24px 100px; }
  .q-number { font-size: 0.78rem; color: var(--muted); margin-bottom: 8px; letter-spacing: 0.05em; text-transform: uppercase; }
  .q-text {
    font-size: 1.1rem; line-height: 1.6; font-weight: 600;
    margin-bottom: 28px; color: var(--text);
  }
  .options { display: flex; flex-direction: column; gap: 12px; }
  .opt {
    background: var(--card); border: 2px solid var(--border);
    border-radius: var(--radius); padding: 16px 20px;
    cursor: pointer; transition: all 0.15s;
    display: flex; gap: 14px; align-items: flex-start;
    font-size: 0.95rem; line-height: 1.5;
  }
  .opt:hover:not(.answered) { border-color: var(--accent); background: var(--card2); }
  .opt .letter {
    background: var(--card2); border-radius: 6px;
    width: 28px; height: 28px; display: flex; align-items: center; justify-content: center;
    font-weight: 800; font-size: 0.8rem; flex-shrink: 0; color: var(--muted);
    transition: all 0.15s;
  }
  .opt:hover:not(.answered) .letter { background: var(--accent); color: white; }
  .opt.correct {
    border-color: var(--correct); background: rgba(34,197,94,0.08);
  }
  .opt.correct .letter { background: var(--correct); color: white; }
  .opt.wrong {
    border-color: var(--wrong); background: rgba(239,68,68,0.08);
  }
  .opt.wrong .letter { background: var(--wrong); color: white; }
  .opt.reveal {
    border-color: var(--correct); background: rgba(34,197,94,0.05);
    opacity: 0.7;
  }
  .opt.reveal .letter { background: var(--correct); color: white; }
  .opt.dimmed { opacity: 0.4; cursor: default; }

  .feedback-box {
    margin-top: 20px; padding: 16px 20px; border-radius: var(--radius);
    font-size: 0.9rem; line-height: 1.5; display: none;
  }
  .feedback-box.show { display: block; }
  .feedback-box.ok { background: rgba(34,197,94,0.1); border: 1px solid rgba(34,197,94,0.3); color: #86efac; }
  .feedback-box.ko { background: rgba(239,68,68,0.1); border: 1px solid rgba(239,68,68,0.3); color: #fca5a5; }

  /* Bottom nav */
  .bottom-nav {
    position: fixed; bottom: 0; left: 0; right: 0;
    background: var(--card); border-top: 1px solid var(--border);
    padding: 16px 24px; display: flex; gap: 12px; justify-content: center; align-items: center;
    z-index: 99;
  }
  .q-counter { font-size: 0.9rem; color: var(--muted); font-weight: 600; min-width: 80px; text-align: center; }

  /* Results */
  .results { max-width: 640px; margin: 40px auto; padding: 0 24px 80px; text-align: center; }
  .results .big-score {
    font-size: 5rem; font-weight: 900; line-height: 1;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    margin-bottom: 8px;
  }
  .results .result-label { font-size: 1.1rem; color: var(--muted); margin-bottom: 32px; }
  .result-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 32px; }
  .result-card {
    background: var(--card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 20px 12px;
  }
  .result-card .val { font-size: 2rem; font-weight: 800; margin-bottom: 4px; }
  .result-card .lbl { font-size: 0.78rem; color: var(--muted); }
  .result-card.ok .val { color: var(--correct); }
  .result-card.ko .val { color: var(--wrong); }
  .result-card.sk .val { color: var(--muted); }
  .review-list { text-align: left; margin-top: 32px; }
  .review-list h3 { font-size: 1rem; font-weight: 700; margin-bottom: 16px; color: var(--wrong); }
  .review-item {
    background: var(--card); border: 1px solid var(--border); border-radius: 10px;
    padding: 14px 16px; margin-bottom: 10px; font-size: 0.88rem; line-height: 1.5;
  }
  .review-item .ri-q { color: var(--text); margin-bottom: 8px; font-weight: 600; }
  .review-item .ri-your { color: var(--wrong); font-size: 0.82rem; }
  .ri-label { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--muted); margin-bottom: 2px; }
  .review-item .ri-correct { color: var(--correct); font-size: 0.82rem; }
  .btn-row { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }
</style>
</head>
<body>

<!-- WELCOME -->
<div id="screen-welcome" class="screen active">
  <div class="welcome">
    <div class="icon">📚</div>
    <h2>38MSEC · <span>Parte específica</span></h2>
    <p><strong>145 preguntas únicas</strong> (145 puntuables y 0 con conflicto entre fuentes). Elige cuántas quieres repasar y el modo de estudio.</p>

    <div class="mode-grid">
      <div class="mode-card selected" data-mode="study" onclick="selectMode('study', this)">
        <div class="mode-icon">🎯</div>
        <