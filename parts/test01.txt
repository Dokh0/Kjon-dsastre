<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>38MSEC – Parte específica · Quiz</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --bg: #0f1117;
    --card: #1a1d27;
    --card2: #22263a;
    --accent: #6c63ff;
    --accent2: #4ecdc4;
    --correct: #22c55e;
    --wrong: #ef4444;
    --text: #e8eaf0;
    --muted: #8b92a9;
    --border: #2d3148;
    --radius: 14px;
  }
  body { background: var(--bg); color: var(--text); font-family: 'Segoe UI', system-ui, sans-serif; min-height: 100vh; }
  
  /* Header */
  .header {
    background: linear-gradient(135deg, #1a1d27 0%, #22263a 100%);
    border-bottom: 1px solid var(--border);
    padding: 16px 24px;
    display: flex; align-items: center; justify-content: space-between;
    position: sticky; top: 0; z-index: 100;
  }
  .header h1 { font-size: 1rem; font-weight: 700; color: var(--text); }
  .header h1 span { color: var(--accent); }
  .header-right { display: flex; gap: 12px; align-items: center; }
  .score-badge {
    background: var(--card2); border: 1px solid var(--border);
    border-radius: 20px; padding: 6px 14px; font-size: 0.82rem;
    display: flex; gap: 8px; align-items: center;
  }
  .score-badge .correct-count { color: var(--correct); font-weight: 700; }
  .score-badge .wrong-count { color: var(--wrong); font-weight: 700; }
  
  /* Progress */
  .progress-wrap { padding: 0 24px; margin: 0; }
  .progress-bar {
    height: 4px; background: var(--card2); border-radius: 4px; overflow: hidden;
  }
  .progress-fill {
    height: 100%; background: linear-gradient(90deg, var(--accent), var(--accent2));
    transition: width 0.4s ease; border-radius: 4px;
  }
  .progress-label {
    display: flex; justify-content: space-between;
    font-size: 0.75rem; color: var(--muted); margin: 6px 0 0 0;
  }

  /* Screens */
  .screen { display: none; }
  .screen.active { display: block; }
  
  /* Welcome */
  .welcome {
    max-width: 600px; margin: 60px auto; padding: 0 24px; text-align: center;
  }
  .welcome .icon { font-size: 3.5rem; margin-bottom: 20px; }
  .welcome h2 { font-size: 2rem; font-weight: 800; margin-bottom: 12px; }
  .welcome h2 span { color: var(--accent); }
  .welcome p { color: var(--muted); font-size: 1rem; line-height: 1.6; margin-bottom: 32px; }
  .mode-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 32px; }
  .mode-card {
    background: var(--card); border: 2px solid var(--border); border-radius: var(--radius);
    padding: 20px 16px; cursor: pointer; transition: all 0.2s; text-align: left;
  }
  .mode-card:hover { border-color: var(--accent); background: var(--card2); }
  .mode-card.selected { border-color: var(--accent); background: var(--card2); }
  .mode-card .mode-icon { font-size: 1.8rem; margin-bottom: 8px; }
  .mode-card h3 { font-size: 0.95rem; font-weight: 700; margin-bottom: 4px; }
  .mode-card p { font-size: 0.78