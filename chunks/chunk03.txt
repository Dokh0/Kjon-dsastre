var(--radius);
    font-size: 0.9rem; line-height: 1.5; display: none;
  }
  .feedback-box.show { display: block; }
  .feedback-box.ok { background: rgba(34,197,94,0.1); border: 1px solid rgba(34,197,94,0.3); color: #86efac; }
  .feedback-box.ko { background: rgba(239,68,68,0.1); border: 1px solid rgba(239,68,68,0.3); color: #fca5a5; }

  /* Bottom nav */
  .bottom-nav {
    position: fixed; bottom: 0; left: 0; right: 0;
    background: var(--card); border-top: 1px solid var(--border);
    padding: 16px 24px; display: flex; gap: 12px; justify-content: center; align-items: center;
    z-index: 99;
  }
  .q-counter { font-size: 0.9rem; color: var(--muted); font-weight: 600; min-width: 80px; text-align: center; }

  /* Results */
  .results { max-width: 640px; margin: 40px auto; padding: 0 24px 80px; text-align: center; }
  .results .big-score {
    font-size: 5rem; font-weight: 900; line-height: 1;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    margin-bottom: 8px;
  }
  .results .result-label { font-size: 1.1rem; color: var(--muted); margin-bottom: 32px; }
  .result-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 32px; }
  .result-card {
    background: var(--card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 20px 12px;
  }
  .result-card .val { font-size: 2rem; font-weight: 800; margin-bottom: 4px; }
  .result-card .lbl { font-size: 0.78rem; color: var(--muted); }
  .result-card.ok .val { color: var(--correct); }
  .result-card.ko .val { color: var(--wrong); }
  .result-card.sk .val { color: var(--muted); }
  .review-list { text-align: left; margin-top: 32px; }
  .review-list h3 { font-size: 1rem; font-weight: 700; margin-bottom: 16px; color: var(--wrong); }
  .review-item {
    background: var(--card); border: 1px solid var(--border); border-radius: 10px;
    padding: 14px 16px; margin-bottom: 10